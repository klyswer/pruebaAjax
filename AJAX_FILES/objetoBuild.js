// Función crear objeto JSON
    function createObject(name, pasw) {//funcion para crear objeto
        var newOjt = {
            nombre: name,
            pass: pasw
        };
        // console.log(newOjt);
        return newOjt;
    }